---
description: Skelio domain package
globs: packages/domain/**/*
alwaysApply: false
---

# Domain (`@skelio/domain`)

- See `docs/04-data-model-schema.md` and `docs/adr/` (add ADRs as decisions are recorded) for time base, coordinates, and JSON conventions.
- Public constant `RUNTIME_SCHEMA_VERSION` must match `schemas/runtime-1.1.0.json` and fixtures under `packages/domain/test/fixtures/`.
- Tests: `pnpm --filter @skelio/domain test`.
