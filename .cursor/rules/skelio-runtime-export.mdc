---
description: Skelio Runtime JSON & Schema
globs: schemas/**/* packages/**/export/**/* packages/**/serializer/**/* **/*export*
alwaysApply: false
---

# Runtime-Export

- Vertrag: `docs/04-datenmodell-schema.md` + `schemas/runtime-1.0.0.json` + `schemas/CHANGELOG.md`.
- Jede Änderung: **ADR** in `docs/adr/`, Schema-Version (SemVer), Fixture unter `packages/domain/test/fixtures/`, CI-/Tests anpassen.
- Keys: **camelCase** (ADR-0008). Zeit: **Sekunden** (`t`) (ADR-0006). `coordinateSystem`: **`y-down`** im MVP (ADR-0007).
