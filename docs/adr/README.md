# Architecture Decision Records (ADR)

Record decisions that affect **runtime export**, **persistence**, or **public APIs**: context, options, and consequences.

**Status:** No numbered ADR files are checked in yet. Add files such as `0001-short-title.md` and link them here when created.

Changing `schemas/runtime-*.json` or export shape requires an ADR, schema semver bump, and fixture/test updates per [AGENTS.md](../../AGENTS.md).
